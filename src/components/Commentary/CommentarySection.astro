---
import SwiperModule from './SwiperModule';
---

<section>
  <div class="commentary-intro">
    <!-- <div class="img-wrapper">
      <img src="/speaker.png" alt="speaker-icon">
    </div> -->
    <h1 class='header'>To moi zuoKlienci...</h1>
    <p class='subheader'>
      "Oni nie są gapy - potrafią liczyć hajs i wiedzą gdzie i z kim robić
      consultingi - gdzie jak włożą tam wyjmą z nawiązką"
    </p>
  </div>
  <SwiperModule client:load />
  <div class='contact-wrapper'>
    <a class='contact-link' href='mailto:kontakt@maciejmartowicz.pl'>
      <span class="email-span">zuokontakt@coachsamozuo.com</span>
    </a>
    <div class='contact-links'>
      <a class='contact-link' href='tel:+48602649588'>
        tel. <span>602-649-588</span> lub
      </a>
      <a
        class='contact-social-icon'
        href='https://www.linkedin.com/in/coachsamozuo-maciek-aloha-marketing-0739a924a/'
      >
        <i class='fa-brands fa-linkedin-in'></i>
      </a>
    </div>
  </div>
</section>

<style>
  section {
    padding: 25px 25px 65px 25px;
    background-color: rgb(229, 227, 233);
  }
  .commentary-intro {
    background-image: url('../../assets/clients_m-full.png');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: contain;
    height: 250px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  .img-wrapper {
    width: 70px;
    margin: 0 auto;
  }
  .img-wrapper img {
    width: 100%;
    opacity: .5;
  }
  .header {
    color: var(--dark-violet);
    font-family: var(--font_3);
    font-size: clamp(1.6rem, 3vw, 3rem);
    letter-spacing: -0.5px;
    text-align: center;
    line-height: 46px;
    margin-top: 10px;
    /* text-transform: uppercase; */
    font-weight: 900;
    padding-bottom: 10px;
  }
  .subheader {
    margin-top: 5px;
    margin-bottom: 25px;
    line-height: 23px;
    text-align: center;
    font-weight: 600;
    font-size: clamp(1rem, 3vw, 2rem);
    color: var(--dark-violet);
    font-style: italic;
  }
  .contact-wrapper {
    margin-top: 25px;
    display: flex;
    gap: 15px;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 15px;
    border-radius: 7px;
    background-color: var(--light-violet);
  }
  .contact-wrapper :is(span) {
    color: var(--base-orange);
    font-size: clamp(0.9rem, 3vw, 4rem);
  }
  .contact-link {
    color: var(--base-white);
    font-family: var(--font_3);
    font-size: clamp(0.9rem, 3vw, 4rem);
  }
  .email-span {
    text-decoration: underline;
  }
  .contact-links {
    display: flex;
    align-items: center;
    gap: 10px; /* Odstęp między elementami */
  }
  .contact-social-icon {
    position: relative;
    font-size: 1rem;
    padding: 19px;
    border-radius: 50%;
    background-color: var(--base-orange);
    transition: 0.3s background-color ease;
  }
  .contact-social-icon:hover {
    background-color: var(--violet);
  }
  .fa-brands {
    position: absolute;
    padding: 19px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--base-white);
  }
 @media (min-width: 900px) {
  .commentary-intro {
    height: 350px;
    /* display: flex; */
    /* flex-direction: column;
    justify-content: center;
    align-items: center; */
  }
 }
 @media (max-width: 410px) {
  .commentary-intro {
    background-position: 0 90px;
    /* background-size: cover; */
  }
 }
</style>
