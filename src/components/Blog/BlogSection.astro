---

---

<section>
  <h2 class='header'>
    Zajrzyj na mój <a href='/blog' class='header-link'>#Blog</a>
  </h2>
  <div class='image-wrapper'>
    <img class='image' src='./blog1.png' alt='' />
  </div>
</section>

<style>
  section {
    padding: 45px 25px 0 25px;
    display: flex;
    align-items: center;
    background-color: var(--base-orange);
  }
  .header {
    widows: 50%;
    font-family: var(--font_3);
    font-size: clamp(2rem, 3vw, 3rem);
    letter-spacing: -1.5px;
    line-height: 32px;
    text-transform: uppercase;
    font-weight: 900;
    padding-bottom: 40px;
    color: var(--base-white);
  }
  .header-link {
    text-decoration: none;
    color: var(--dark-violet);
    font-size: 2.4rem;
  }
  .image-wrapper {
    display: flex;
    width: 100vw;
    overflow: hidden;
  }
  .image {
    display: flex;
    width: 100%;
  }
</style>
