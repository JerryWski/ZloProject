---

---

{
  (
    <div class='card'>
      <slot />
    </div>
  )
}

<style>
  .card {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    margin-bottom: 20px;
    gap: 20px;
    color: var(--dark-violet);
    font-family: var(--font_3);
  }
</style>
