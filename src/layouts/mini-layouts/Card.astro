---

---

{
  (
    <div class='card'>
      <slot />
    </div>
  )
}

<style>
  .card {
    display: flex;
    justify-content: center;
    /* align-items: center; */
    flex-direction: column;
    padding-top: 40px;
    padding-bottom:40px;
    gap: 10px;
    color: var(--dark-violet);
    font-family: var(--font_3);
  }
</style>
